import 'package:flutter/material.dart';

class FoodScheduleScreen extends StatelessWidget {
  const FoodScheduleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
